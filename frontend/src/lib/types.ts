export interface UserType {
    id: number,
    username: string,
    role: string,
}

export interface ProductType {
    id: number,
    name: string,
    description: string,
    price: number,
    stock: number,
}
